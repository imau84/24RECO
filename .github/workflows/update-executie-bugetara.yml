name: Update Execuție Bugetară (Ministerul Finanțelor)

on:
  schedule:
    # în fiecare luni, 06:00 UTC
    - cron: "0 6 * * 1"
  workflow_dispatch: {}

permissions:
  contents: write

jobs:
  update:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Instalează dependențe
        run: pip install requests openpyxl

      - name: Rulează scriptul de fetch
        run: python scripts/fetch_executie_bugetara.py

      - name: Commit & push dacă există modificări
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          if ! git diff --quiet public/executie-bugetara_data.json; then
            git add public/executie-bugetara_data.json
            git commit -m "auto: actualizare date Execuție Bugetară MF"
            git push
          else
            echo "Nicio modificare."
          fi
