import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Transport marfă în România — operatori și autorizații",
    description: "Piața transportului rutier de marfă din România: operatori licențiați și autorizații. Date publice, actualizate lunar.",
      alternates: { canonical: "/transport" },
      };

      export default function Layout({ children }: { children: React.ReactNode }) {
        return children;
        }
        
